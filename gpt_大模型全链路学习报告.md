# 大模型全链路学习计划：本机优先、低资源、16 周实践版

> **制定日期：2026 年 9 月 18 日**  
> **核心目标：** 不止会调用模型，而是理解并实现数据准备、分词、模型、预训练、后训练、推理与反馈迭代的关键链路。  
> **执行原则：** 缩小模型和数据，不跳过关键机制；先验证正确性，再追求速度和效果。  
> **推荐组合：** Raschka 代码主线 + CS336 理论与系统主线 + smol-course/TRL 后训练实践；中文资料按需补充。

---

## 目录

1. [目标、前提与完成边界](#s1)
2. [课程清单与使用顺序](#s2)
3. [硬件分档与低资源配置](#s3)
4. [16 周路线总览](#s4)
5. [每周详细任务与验收](#s5)
6. [三个贯穿项目](#s6)
7. [环境、目录与实验记录](#s7)
8. [资源控制与故障排查](#s8)
9. [结课验收与进阶方向](#s9)
10. [课程、代码和官方文档入口](#s10)

<a id="s1"></a>
## 一、目标、前提与完成边界

### 1.1 学完后应该能做什么

能够从一段文本开始，说明它如何变成 token、进入 Transformer、计算损失、反向传播、更新权重，并最终生成文本；能够独立完成小规模训练与验证，保存和恢复检查点；能够对现成小模型进行 SFT/LoRA 与偏好优化，理解可验证奖励强化学习的反馈过程；能够测量推理性能，并将模型放入一个可测试的本地任务环境。

**衡量标准不是“看完多少视频”，而是“写出了什么、通过了哪些测试、能解释哪些结果”。**

### 1.2 本计划的假设

你的具体操作系统、GPU 型号、显存和内存容量尚未明确，因此不预设某一台机器一定能运行某个配置。

时间按**每周约 8—10 小时，共 128—160 小时**安排。这是学习投入建议，不是完成保证。默认能够阅读和修改 Python；PyTorch 不熟时，先补附录 A 和第 1 周任务。基础不足可增加 2—4 周；每周只有 4—5 小时，可把每个学习周拆成两个自然周。

以下模型大小、数据量、周次和验收条件均为**本计划设计的教学配置**，不是所引用课程的原版作业要求，也不是对你的硬件进行实测后的结论。

### 1.3 分清三种完成程度

| 层级 | 要求 | 能据此得出的结论 |
|---|---|---|
| **L1：机制验证，必做** | 小张量测试、微型模型、少量样本；真实执行前向、损失、反向与更新 | 我理解并实现了这个机制。 |
| **L2：小模型实训，按资源完成** | 用已有预训练能力的小模型做 SFT、DPO 或规则奖励训练，并在独立数据上评估 | 我完成了小规模训练和可复核对照。 |
| **L3：规模与性能复现，进阶** | 原版规模训练、GPU 内核、多卡、复杂在线 Agent-RL | 我在明确的设备和配置上复现了相应结果。 |

**CPU 路线可以完成大量 L1 实验，但不能据此声称完成了所有 L2/L3。** 仅推理、仅计算损失、仅阅读代码，也不能记为“完成训练”。无法完成的实训应明确标注，不需要为了勾满清单购买大显卡。

### 1.4 两条模型路线并行

```text
路线 A：原理实现
小语料 → 自己实现分词/模型 → 微型预训练 → 验证/恢复 → KV cache 与性能测试

路线 B：能力利用
已有小模型 → 任务基线 → LoRA-SFT → DPO 或规则奖励实验
                                  ↓
                     本地工具环境 → 收集轨迹 → 筛选数据 → 再训练
```

路线 A 学内部机制，不追求通用聊天质量；路线 B 利用已有能力做真实任务。不要强求从零训练的微型模型立即成为好用的助手。

<a id="s2"></a>
## 二、课程清单与使用顺序

### 2.1 核心课程与代码

| 资源 | 类型与公开入口 | 在本计划中的用途 | 需要注意 |
|---|---|---|---|
| **Raschka：Build a Large Language Model From Scratch** | [官方代码仓库][R1]；分章代码、Notebook、练习 | 第 1—8 周的代码主线；重点第 2—5、7 章及附录 A、E | 代码公开不等于书籍和配套视频全部免费。作者说明主章节代码面向普通笔记本；扩大模型与数据后的成本需重新评估。 |
| **Stanford CS336：Language Modeling from Scratch** | [2026 官网][R2]；课表、录像入口、讲义和作业仓库 | 理论与系统骨架；按主题配合学习 | 原版包含 GPU 内核与分布式实验。本计划缩小训练规模，不等价于完成原版作业。 |
| **Hugging Face smol-course** | [官方仓库][R3]；课程材料与 Notebook | 指令训练、评估、偏好优化 | 当前版本默认示例不都采用 135M 小模型；先读配置。主线选已公开单元，不把课程计划表中的发布日期当成全部材料已验证。 |
| **MiniMind** | [作者仓库][R4]；中文端到端代码项目 | 对照阅读预训练、SFT、LoRA、DPO 等阶段如何衔接 | 它是项目，不是大学课程；不直接套用宣传中的训练时间和费用。 |
| **Happy-LLM** | [Datawhale 仓库][R5]；中文教程和实践代码 | 中文补充，重点架构、搭建模型、训练实践 | 不要求全书照做；各章依赖和运行位置分别确认。 |
| **清华唐杰“高级机器学习”新版** | [教师官方主页][R6]；你提供的小红书笔记作为课程线索 | 保留为全链路候选主课；拿到新版正式资料后映射到本计划 | **本次尚未核实完整新版官方录像、作业和代码集合，不用 2024 归档代替新版。** |

上述公开内容依据各官方页面/作者仓库；表中的学习顺序与取舍是本计划建议。[R1][R2][R3][R4][R5]

**关于唐杰新版：** 本计划不把此前对话中的媒体转述，当作已经核验的正式教学大纲。先用已公开材料开始学习；正式新版材料取得后，只替换相应讲解或作业，不同时重做两套全课。

### 2.2 按需补充，不再增加第二条必修主线

| 资源 | 什么时候用 | 入口 |
|---|---|---|
| Karpathy：Neural Networks: Zero to Hero | 反向传播、语言建模或 GPT 实现不熟时 | [视频与代码入口][R7] |
| Hugging Face LLM Course | 需要查 Transformers、Datasets、Tokenizer 等工具用法时 | [官方课程][R8] |
| 李宏毅 Machine Learning 2026 | 需要中文解释推理加速、KV cache、位置编码、Agent 等主题时 | [课程官网][R9] |
| TRL / PEFT 官方文档 | 实际写 SFT、LoRA、DPO、GRPO 时核对参数和数据格式 | [SFT][D1] / [LoRA][D2] / [DPO][D3] / [GRPO][D4] |

这些资源的主题和公开入口可从对应主页核实；李宏毅课程部分作业使用校内平台，不默认校外可取得所有作业内容。[R7][R8][R9]

### 2.3 执行顺序

**先写 Raschka 的关键模块，再读 CS336 对应主题；中文理解有障碍时补 Happy-LLM 或李宏毅；进入后训练后以 smol-course 与官方文档为主。**

MiniMind 用于第二轮串联源码，不作为第一周的“安装后一键训练”。同一知识点只选一套主讲解，写不出来时再查第二套。

<a id="s3"></a>
## 三、硬件分档与低资源配置

### 3.1 根据本机选择路线

以下为保守的实验起点，**不是兼容性或显存容量保证**；同容量显卡也可能因架构、精度和算子支持产生差异。

| 本机情况 | 默认安排 | 小模型后训练建议 | 不作为必做 |
|---|---|---|---|
| 无独显，约 16GB 系统内存 | 微型模型、分词、损失、训练循环、数据处理、本地检索 | 先用微型模型学机制；135M 的极少步适配须先测速度 | 完整在线 RL、多卡、GPU 内核性能 |
| NVIDIA 约 4GB 显存 | 微型模型与小语料真实训练 | 优先 135M LoRA；0.6B 量化方案先做容量试验 | 默认 0.6B DPO/GRPO、长上下文 |
| NVIDIA 6—8GB 显存 | 路线 A 的小规模完整实验 | 从 135M 起步；再试 0.6B LoRA/QLoRA，batch 1、短序列 | 原版 CS336 完整规模、复杂 Agent-RL |
| NVIDIA 12—16GB 显存 | 保持同一路线，增加数据预算与复现实验 | 优先把 0.6B 实验做扎实，不急着上 7B | 为追求“大模型”而扩大到无法对照的规模 |
| Apple Silicon，16/24GB 统一内存 | PyTorch 小实验；推理和微调可研究 MLX-LM | 按 MLX 支持范围选择模型与量化配置 | 直接照搬 CUDA、bitsandbytes 或 Triton 的配置 |

MLX-LM 提供 Apple Silicon 上的模型推理和微调能力；统一内存仍与系统共享，不能与同容量独立显存简单等同。[D7]

### 3.2 路线 A 的建议配置

从 **A0** 开始；只有在测试通过、速度和内存可接受时才扩大。下面是配置设计，不是任何课程仓库可直接读取的命令行参数。

| 配置 | 层数 | 隐藏维度 | 注意力头数 | 词表规模 | 上下文长度 | 单次 batch | 用途 |
|---|---:|---:|---:|---:|---:|---:|---|
| A0 | 2 | 128 | 4 | 512 | 128 | 1—2 | CPU 验证、单 batch 过拟合、基础训练 |
| A1 | 4 | 256 | 4 | 2,048 | 256 | 1 | 小显卡实验与小规模对照 |
| A2 | 6 | 384 | 6 | 4,096 | 256 | 1 | 资源有余量后的扩展，不是必做 |

A0 可先用字节级分词；使用 BPE 时，词表只在训练集上学习。初版用常规残差结构、LayerNorm 和位置嵌入，跑通后再逐一加入 RMSNorm、RoPE 等变体。不要同时改架构、分词和数据，导致结果无法解释。

### 3.3 路线 B 的建议模型

| 模型 | 推荐用途 | 边界 |
|---|---|---|
| [SmolLM2-135M-Instruct][M1] | 英文短任务、小样本 SFT/LoRA、偏好数据实验 | 模型卡主要标注英文；不把其中文效果当作训练方法的公平评价。 |
| [Qwen3-0.6B][M2] | 中文短文本字段抽取、JSON 输出、本地小任务 | 第一轮关闭 thinking 并限制输出；不要求小模型胜任长程复杂 Agent。 |

Qwen3-0.6B 模型卡提供 `enable_thinking=False` 的非思考模式开关。[M2]

**不要混淆模型身份：** Instruct 或已经后训练的模型是“已有模型基线”，不是未经对齐的预训练基座。若研究“预训练基座到指令模型”的变化，需要另选明确的 Base 版本并记录来源。

### 3.4 首轮微调参数起点

```yaml
# 教学实验建议，不是可直接交给任意 Trainer 的完整配置。
model_size: 135M_or_0.6B
max_sequence_length: 256
micro_batch_size: 1
gradient_accumulation_steps: 8
lora_rank: 8
learning_rate_trial: 0.0001
smoke_test_samples: 50
smoke_test_steps: 20
max_new_tokens: 64
precision: choose_after_hardware_check
```

先验证数据和 loss，再试学习率、LoRA rank 和训练轮次。DPO、GRPO 的数据处理、参考模型与采样要求不同，不能原样照搬这份 SFT 起点配置。[D1][D3][D4]

<a id="s4"></a>
## 四、16 周路线总览

| 周次 | 主题 | 主资源 | 主要产物 |
|---|---|---|---|
| 第 1 周 | 环境、张量、反向传播、最小训练 | Raschka 附录 A | 环境记录、微型训练测试 |
| 第 2 周 | 文本、分词、数据切分、样本生成 | Raschka 第 2 章；CS336 Basics | 分词器、数据加载器及测试 |
| 第 3 周 | 注意力、因果掩码、多头注意力 | Raschka 第 3 章 | 可解释、可测试的注意力实现 |
| 第 4 周 | Decoder-only Transformer | Raschka 第 4 章 | 自己的微型语言模型 |
| 第 5 周 | 训练循环、调度、保存与恢复 | Raschka 第 5 章 | 完整预训练脚本与日志 |
| 第 6 周 | 数据质量、评估、预算对照 | CS336 Data/Scaling；对应源码 | 小规模数据与预算实验报告 |
| 第 7 周 | 任务数据、基线、SFT | smol-course；Raschka 第 7 章 | 数据集、基线、SFT 实验 |
| 第 8 周 | LoRA、量化、适配器管理 | Raschka 附录 E；PEFT | LoRA 训练与加载验证 |
| 第 9 周 | 偏好数据、DPO | smol-course；TRL DPO | 偏好对、损失测试、小模型对照 |
| 第 10 周 | 可验证奖励与最小在线 RL | TRL GRPO；MiniMind 对照源码 | 验证器、采样日志、真实更新实验 |
| 第 11 周 | KV cache、推理过程 | CS336 Inference；中文补充 | 缓存实现与数值一致性测试 |
| 第 12 周 | profiling、量化推理、部署 | CS336 Systems；llama.cpp/MLX | 延迟、吞吐、内存与质量报告 |
| 第 13 周 | 可验证的本地工具环境 | 自建小项目；按需查文档 | 有边界的任务环境与工具接口 |
| 第 14 周 | 轨迹、失败分析、固定基准 | 综合项目 | 轨迹集、错误分类、成功率统计 |
| 第 15 周 | 筛选数据、轨迹 SFT、选做持续学习 | 后训练代码复用 | 一次可复核的迭代实验 |
| 第 16 周 | 消融、复现、总结 | 全部产物 | 仓库、报告、演示与下一步计划 |

**节奏建议：** 每周约 2—3 小时阅读/视频、4—5 小时编码、1—2 小时测试和复盘。单周任务较多时，优先完成验收项，进阶项顺延。

<a id="s5"></a>
## 五、每周详细任务与验收

以下任务是围绕课程主题设计的自学练习；建议文件名代表你将创建的产物，不表示本 Markdown 已附带完整项目源码。

### 第 1 周：先打通最小训练，不急着下载大模型

**学习材料：** [Raschka 附录 A][R1]；反向传播不熟时补 [Karpathy][R7]。

**输入 → 处理 → 输出：** 小型数值/文本样本 → 前向、loss、反向、优化器更新 → 参数变化与训练日志。

完成张量形状、索引、广播、矩阵乘法和梯度实验。用极小网络或微型字符模型训练同一个 batch；记录 loss、梯度和更新前后权重。这里刻意过拟合，是为了验证训练代码，不用它评估泛化。

**资源安排：** CPU；小数据；不依赖外部模型 API。

**产物：** `check_env.py`、`tests/test_gradients.py`、`reports/week01.md`。

- [ ] 能解释 `backward()`、`step()`、`zero_grad()` 各做什么。
- [ ] 梯度非空且有限；训练参数确实变化。
- [ ] 能让一个 batch 的 loss 明显下降。
- [ ] 记录本机配置、PyTorch 版本及可用设备。

### 第 2 周：分词与数据管线

**学习材料：** [Raschka 第 2 章][R1]、[CS336 A1][A1]。

**输入 → 处理 → 输出：** 公开或自制小语料 → 文档级切分、分词、窗口采样 → 输入 token 与右移目标。

先实现字节级分词，再做教学版 BPE。保留原文与文档 ID；按文档划分训练/验证/测试，检查重复或近重复，不让同一文档的不同窗口跨集合。Tokenizer 只用训练集拟合。

训练数据的窗口应满足：输入 `tokens[i:i+T]`，目标 `tokens[i+1:i+T+1]`。

**资源安排：** CPU；先处理几十 KB，正确后再到几 MB；不要下载完整网页语料库。

**产物：** `tokenizer.py`、`data.py`、`tests/test_tokenizer.py`、数据清单。

- [ ] 非特殊文本能正确编码再解码，包含中文与换行测试。
- [ ] 目标恰好右移一位，没有右移两次。
- [ ] 同源文档不跨训练与测试；重复检测结果已记录。
- [ ] 能解释词表大小与文本 token 数如何影响实验。

### 第 3 周：真正写清楚注意力

**学习材料：** [Raschka 第 3 章][R1]；按需配合 [Happy-LLM][R5]。

**输入 → 处理 → 输出：** `[B,T,d]` 表示 → Q/K/V 投影、相似度、掩码、softmax、加权汇总 → 同长度新表示。

实现单头和多头注意力，打印每一步形状。构造一个只有几个 token 的例子，检查因果掩码、缩放和 softmax 的维度。先用清晰实现，不加 FlashAttention。

**资源安排：** CPU；`B=2, T=8, d=32` 之类的小测试。

**产物：** `attention.py`、`tests/test_attention.py`。

- [ ] 修改未来位置的输入，不影响更早位置的输出。
- [ ] 上述测试在关闭 dropout 后执行。
- [ ] 每个注意力头的形状与合并方式正确。
- [ ] 无 NaN，能解释为何不能看未来。

### 第 4 周：组装自己的 Transformer

**学习材料：** [Raschka 第 4 章][R1]、[CS336 A1][A1]。

**输入 → 处理 → 输出：** token ID → 嵌入、位置、若干 Transformer block、输出层 → `[B,T,V]` logits。

实现 decoder-only 模型、残差、归一化、前馈网络与生成函数。先保持一个最简单的正确架构；RoPE、RMSNorm、SwiGLU 只选一个作为附加练习，不同周分开改。

**资源安排：** 使用 A0；在同一 batch 上检查训练行为。

**产物：** `model.py`、`generate.py`、`tests/test_model.py`、参数统计。

- [ ] logits 形状、词表范围和 loss 输入匹配。
- [ ] 保存并加载后，在相同模式和输入下输出一致。
- [ ] 整个模型也通过因果性测试。
- [ ] 能画出一个 token 从嵌入到 logits 的处理路径。

### 第 5 周：把训练做成可恢复的实验

**学习材料：** [Raschka 第 5 章及附录 D][R1]。

**输入 → 处理 → 输出：** 模型、训练配置、样本流 → 优化、验证、检查点 → 可恢复权重与日志。

实现训练/验证模式切换、梯度累积、学习率调度、梯度裁剪和检查点。记录训练步、实际 token 数、耗时、验证 loss；检查点包含模型、优化器、调度器、步数与随机状态。需要精确重现时，另保存数据顺序/采样位置。

**资源安排：** 先做 20 步检查，再做几百步；训练规模由实际 tokens/s 决定。

**产物：** `train.py`、`checkpoint.py`、一组真实日志和恢复测试。

- [ ] 中断后能继续，不会悄悄重新初始化优化器。
- [ ] 验证不更新权重、不累积梯度。
- [ ] 累积边界和末尾不足一组的 batch 有明确处理。
- [ ] 报告区分训练 loss、验证 loss 和生成样例。

### 第 6 周：数据质量与计算预算

**学习材料：** [CS336 A4][A4]、[CS336 A3][A3]。

**输入 → 处理 → 输出：** 同一数据来源的不同处理版本 → 固定条件训练 → 质量/成本对照。

完成两类中的至少一类实验：其一，原始训练数据与去重清洗版比较；其二，两个小模型规模在相近 token 预算下比较。保持 tokenizer、测试集和评价方法一致。另可比较固定训练时长，但不要把“固定 token”和“固定计算预算”混为一谈。

**资源安排：** 优先两组小实验；资源有余量再增加重复种子。少量点只用于理解预算关系，不拟合并外推所谓可靠的大模型 scaling law。

**产物：** 数据版本清单、实验对照表、`reports/pretrain_ablation.md`。

- [ ] 清洗改变了什么、删除了多少数据有记录。
- [ ] 测试集不参与清洗阈值调优。
- [ ] 不跨不同 tokenizer 直接比较 token 级 perplexity。
- [ ] 能说明当前结果支持什么、不支持什么。

### 第 7 周：构造任务数据并进行 SFT

**学习材料：** [smol-course][R3]、[Raschka 第 7 章][R1]、[TRL SFT][D1]。

**输入 → 处理 → 输出：** 短文本与标准答案 → 聊天模板、响应 loss、微调 → 任务模型与评估结果。

建议任务为“从公开技术资讯中抽取企业、事件、明确日期、原文证据”。先用 50 条样本检查数据；之后可扩展为 300—1,000 条训练数据，另保留开发集和至少 100 条独立测试数据。不要在本周追求大规模人工标注。

先评估未微调模型，再训练。检查 system/user、padding、assistant 各部分的 loss mask；`assistant_only_loss` 依赖所用聊天模板支持，不仅仅是打开一个布尔值。[D1]

**资源安排：** CPU/4GB 优先 135M 的极小实验；中文任务可尝试 0.6B。显存不足时本周直接使用 LoRA，下周再拆解其原理；不强制先做全参数微调。

**产物：** `train.jsonl`、`dev.jsonl`、`test.jsonl`、基线与 SFT 记录。

- [ ] 训练样本、聊天模板与截断后文本已人工抽查。
- [ ] assistant 的有效标签不全是忽略值。
- [ ] padding 按位置掩码；若 pad 与 EOS 共用 ID，不误删真实 EOS 标签。
- [ ] 同一测试集比较基线与训练后模型，保留失败结果。

### 第 8 周：LoRA 原理、适配器与量化

**学习材料：** [Raschka 附录 E][R1]、[PEFT LoRA][D2]、[bitsandbytes 文档][D5]。

**输入 → 处理 → 输出：** 固定基础权重和任务数据 → 训练低秩增量 → 可独立保存/加载的适配器。

先在小线性层上实现 `W' = W + (alpha/r) * B @ A`，检查形状与梯度；再阅读 PEFT 中的目标模块、rank、缩放与冻结设置。核查实际模块名，不把别的架构的 `target_modules` 原样照搬。

适配器需要与正确的模型版本、tokenizer、聊天模板一起使用。能够运行普通 LoRA 时，不要为了“更先进”强行增加量化依赖。

**资源安排：** 一次只比较一个变量，例如 rank 4 与 rank 8。QLoRA 是兼容硬件上的选做；4-bit 权重不代表全部训练内存都为 4-bit。[D5]

**产物：** 小矩阵 LoRA 测试、适配器、加载脚本、参数/内存记录。

- [ ] 可训练参数的数量和名称符合预期。
- [ ] 冻结权重未更新，适配器权重发生变化。
- [ ] 重新加载后的输出与保存前基本一致。
- [ ] 解释 LoRA 与 QLoRA 改变了哪些对象。

### 第 9 周：DPO——从偏好对到训练信号

**学习材料：** [smol-course 偏好单元][R3]、[TRL DPO][D3]、[MiniMind 对应源码][R4]。

**输入 → 处理 → 输出：** 同一问题的 chosen/rejected 回答 → 策略/参考模型概率比较 → 偏好优化后的参数。

先整理 30 对用于检查，再增加到 100—300 对。偏好尽量基于“有证据/无证据、字段正确/错误”，而不是简单偏爱更长的答案。先用小张量算一次 DPO loss，再调用训练工具。

标准 sigmoid DPO 的教学表达为：

```text
delta = [log πθ(chosen|x) - log πref(chosen|x)]
      - [log πθ(rejected|x) - log πref(rejected|x)]
loss  = -log sigmoid(beta * delta)
```

概率仅累计有效回答 token，忽略 prompt/padding。参考模型固定，并明确它究竟是原始模型还是 SFT 检查点；简单“关闭 SFT 适配器”不一定等于你希望使用的 SFT 参考模型。具体实现以固定版本的 DPO 文档为准。[D3]

**资源安排：** 短序列、小模型；按文档研究预计算参考 log-prob 的适用条件，不能在参考模型改变后继续使用旧缓存。[D3]

**产物：** 偏好数据、loss 测试、DPO 训练/评估报告。

- [ ] chosen 与 rejected 属于同一个 prompt，且标签经过抽查。
- [ ] 参考参数冻结，policy 参数更新。
- [ ] 对照 SFT 与 SFT+DPO，报告字段准确率、格式和长度变化。
- [ ] 明确：DPO 不等于跑通在线 RLHF，也不保证效果改善。

### 第 10 周：可验证奖励与在线采样更新

**学习材料：** [TRL GRPO][D4]；可对照 [MiniMind][R4] 阅读。

**输入 → 处理 → 输出：** 题目 → 当前模型采样 → 程序验证 → 奖励/优势 → 参数更新。

选择一个简单、答案明确的任务，例如短整数排序或两位数加法。程序生成训练题和不重叠测试题；验证器检查格式和答案，正确性奖励应比仅格式奖励更重要。避免只提取最后一个数字，导致带错误内容的输出也得高分。

本周与企业资讯任务是**独立教学实验**。不把算术上的 RLVR 结果和资讯抽取上的 DPO 结果混成算法优劣比较。GRPO 是算法选项，RLVR 描述可验证奖励的训练路线，两者不是同义词。[D4]

**资源安排：** 先用微型任务模型完成真实策略梯度更新；资源允许再用小型预训练模型做 GRPO。每题先采样 2 个短答案；正式 Trainer 的生成组数需与有效生成 batch 约束匹配。CPU 未做完整 GRPO 时，诚实标注“完成最小策略梯度实验，未完成 GRPO 实训”。

**产物：** `verifier.py`、奖励测试、采样记录、真实更新日志。

- [ ] 验证器经过正确、错误、空串、恶意格式等边界测试。
- [ ] 记录每题的回答、奖励以及是否存在组内奖励差异。
- [ ] 真正发生参数更新；纯采样打分不算训练完成。
- [ ] 回测固定测试集；奖励上涨不自动等于能力提高。

### 第 11 周：实现 KV cache

**学习材料：** [CS336 推理主题][R2]；[李宏毅推理加速内容][R9]；Raschka 仓库的相关补充材料。[R1]

**输入 → 处理 → 输出：** prompt 和已有 K/V → 增量前向 → 下一 token 与更新后的缓存。

回到路线 A 的手写模型，先实现 prefill，再实现逐 token decode；明确每层缓存形状、位置偏移和有效上下文。比较无缓存与有缓存，而不是仅打开现成库的 `use_cache`。

**资源安排：** CPU 可做正确性测试；速度差异在同一设备、相同长度下测量。

**产物：** `cache.py`、`tests/test_cache.py`、缓存内存统计。

- [ ] eval 模式下缓存与非缓存 logits 在合理容差内一致。
- [ ] 固定解码条件下，生成序列一致或数值差异有解释。
- [ ] 多层缓存、位置偏移、上下文上限均有测试。
- [ ] 能解释为什么缓存减少重复计算，却仍占内存。

### 第 12 周：性能测量、量化推理与本地接口

**学习材料：** [CS336 A2][A2]、[llama.cpp][D6]、[MLX-LM][D7]。

**输入 → 处理 → 输出：** 固定模型与测试输入 → 推理、测时、内存采集 → 性能与质量对照。

测量首 token 延迟、decode tokens/s、峰值内存和任务质量。GPU 测时需处理异步执行并预热；冷启动和模型加载单独记录。量化推理使用兼容的现成模型，核实转换来源，不要求把手写模型强行导入 GGUF。

**资源安排：** CPU 优先 llama.cpp 兼容路线；Apple 可研究 MLX。Triton、FlashAttention、多卡通信仅作进阶，不用 CPU 时间替代 GPU benchmark。

**产物：** `benchmark.py`、本地命令行/接口、`reports/inference.md`。

- [ ] 比较时固定 prompt token 长度、输出上限、线程和 batch。
- [ ] 同时报性能与输出质量，不只说“量化更快”。
- [ ] 记录权重格式、精度、工具版本和测试设备。
- [ ] 仅推理工具没有被误写成训练工具。

### 第 13 周：搭建小而可验证的本地 Agent 环境

**学习材料：** 复用已实现模型接口；按需查 [Happy-LLM 应用章节][R5] 和 [李宏毅课程][R9]。

**输入 → 处理 → 输出：** 查询任务 → 模型选择工具 → 本地检索/读取 → 结构化答案与证据。

只提供三个接口：`search_docs(query)`、`read_doc(doc_id)`、`submit_answer(answer)`。检索可先用关键词或 TF-IDF，不要求向量数据库。模型输出 JSON 动作，由程序严格解析、校验、执行并返回 observation。

采用本地公开文档或合成文档，不访问真实账户；限制为 3—5 次工具调用。禁止直接执行模型产生的任意 Python/shell，文档内容不得覆盖系统规则。

**资源安排：** 本地小模型；短文档、短输出；不引入多 Agent 框架。

**产物：** `environment.py`、`tools.py`、任务定义和端到端轨迹。

- [ ] 非法工具名、路径和参数被拒绝。
- [ ] 找不到证据时有拒答或未知值，不伪造文档。
- [ ] 超步数、超时和格式错误有明确终止状态。
- [ ] 能区分检索失败、工具调用失败和生成错误。

### 第 14 周：建立固定评测与失败分析

**输入 → 处理 → 输出：** 固定任务集 → 统一执行器 → 成功率、证据正确率、成本与错误标签。

设计开发任务与最终测试任务。先比较“固定检索流程”和“模型自主选择工具”，再讨论 Agent 是否有收益。统一记录每步动作、observation、最终回答、token 数、工具次数和用时。

证据评价至少检查文档 ID、引用片段存在性和字段是否被支持。仅“片段出现在原文”不足以证明语义支持，复杂样例需要人工复核。

**资源安排：** 开发阶段可用 30—50 个任务；最终测试尽量保留至少 100 个独立任务。小样本结果报告成功数/总数，不夸大微小百分比差异。

**产物：** `evaluate.py`、`trajectories.jsonl`、错误分析报告。

- [ ] 同一执行器和规则评价所有模型版本。
- [ ] 测试任务不用于修改奖励、提示或数据筛选阈值。
- [ ] 有简单固定流程基线，不默认 Agent 更优。
- [ ] 随机生成的结果在条件允许时做重复试验。

### 第 15 周：用经过筛选的轨迹进行一次再训练

**输入 → 处理 → 输出：** 开发环境轨迹 → 程序校验与人工抽查 → 训练样本 → 新适配器。

只使用训练/开发环境产生的轨迹，保留来源。成功轨迹转为 SFT 数据，失败轨迹用于分析或构造经过核验的偏好对。工具输出不应无差别地当成模型目标文本；明确哪些 action/answer token 参与训练。

对照“训练前模型”和“加入筛选轨迹后的模型”，必要时增加等量原始数据继续 SFT 的基线，避免把额外训练时长误认为方法收益。

**资源安排：** 复用 LoRA。持续学习选做：分别准备 A/B 两个小任务，训练 B 后回测 A；检索记忆增加不等于参数层面的持续学习。

**产物：** 筛选记录、轨迹训练集、新适配器、迭代对照。

- [ ] 测试答案或最终测试轨迹没有进入训练。
- [ ] 原始轨迹、筛选依据和人工抽查记录可追溯。
- [ ] 结果区分收益、退步与无显著变化。
- [ ] 明确称为“轨迹筛选 + SFT”；没有在线策略更新，不称为在线 Agent-RL。

### 第 16 周：完成可复现的结课交付

**输入 → 处理 → 输出：** 全部代码、数据说明、实验记录 → 清理与复测 → 可复现仓库和总结报告。

整理训练、评估、推理的入口；重新创建环境验证关键步骤。报告任务定义、数据划分、方法、配置、硬件、结果、失败分析、资源成本与限制。保留负结果，不为了展示效果只挑成功样例。

**资源安排：** 只重跑关键实验，不在最后一周更换大模型或增加复杂框架。

**产物：** `README.md`、依赖记录、测试、配置、最终报告、简短演示。

- [ ] 新环境按 README 能复现最小训练与评估。
- [ ] 每个结果都能定位到代码版本、数据版本和配置。
- [ ] 清楚标注 L1/L2/L3 的实际完成情况。
- [ ] 用自己的话讲清从文本到更新、从反馈到再训练的全过程。

<a id="s6"></a>
## 六、三个贯穿项目

### 项目 A：TinyLM Lab——自己的训练实验室

**范围：** 第 1—6 周构建，第 11—12 周补推理优化。

输入小语料，输出自己实现的 tokenizer、Transformer、训练循环、检查点和生成程序。验收重点是正确性、实验记录和可解释性，不以聊天流畅度作为首要标准。

最少保留因果性、标签右移、梯度、保存恢复、缓存一致性五类测试，以及一次训练预算或数据处理对照。

### 项目 B：领域结构化抽取——SFT 与 DPO 实验

**范围：** 第 7—9 周；使用已有预训练能力的模型，不要求由项目 A 的权重起步。

建议输出字段：`company`、`event_type`、`event_date`、`evidence`。只抽取原文明确表达的日期；无法确定时为 `null`。

下面是**虚构教学样例**，不是现实企业事件：

```json
{
  "text": "示例甲公司于2026年5月8日发布一款工业检测设备。",
  "answer": {
    "company": "示例甲公司",
    "event_type": "产品发布",
    "event_date": "2026-05-08",
    "evidence": "示例甲公司于2026年5月8日发布一款工业检测设备"
  }
}
```

训练时把它转换成所用 Trainer 支持的对话或 prompt-completion 格式；上面的业务 JSON 不是直接通用的训练数据格式。[D1]

对比至少三项：已有模型基线、LoRA-SFT、SFT+DPO。度量字段准确率、JSON 有效率、证据支持率、未知信息的正确处理和资源开销。按文档/事件分组切分，不让同一新闻的改写同时进入训练与测试。

### 项目 C：本地检索—工具调用—轨迹迭代

**范围：** 第 13—16 周；复用项目 B 的任务和模型接口。

```text
用户任务
  ↓
本地查询/阅读工具
  ↓
模型输出答案与证据
  ↓
程序验证 + 人工抽查
  ↓
记录成功与失败轨迹
  ↓
筛选训练数据 → LoRA 再训练 → 独立测试
```

这个项目优先采用公开、合成或有授权的文档，不上传客户、员工或内部业务数据到外部 API。第一版不做浏览器控制、不连接真实业务账户，也不让模型执行任意代码。

**学习边界：** 将模型接进工具循环属于 Agent 执行；把轨迹筛选后做 SFT 属于模仿/监督训练；在线按奖励更新策略才属于相应的 Agent-RL 实验。三个层次分别记录。

<a id="s7"></a>
## 七、环境、目录与实验记录

### 7.1 先做环境检查

第一个环境只装项目 A 所需基础依赖。PyTorch 按官方安装选择器匹配操作系统和计算平台，不凭显卡名称猜 CUDA 安装命令。[D8]

```bash
python -m venv .venv

# Windows PowerShell，执行策略允许时：
.\.venv\Scripts\Activate.ps1

# Linux/macOS：
source .venv/bin/activate

# 激活命令二选一。随后按 PyTorch 官网安装 torch，再装基础工具：
python -m pip install numpy matplotlib pytest
```

已安装 PyTorch 后，把下面代码保存为 `check_env.py` 并运行。它只检查当前环境，不下载模型。

```python
import platform
import sys

try:
    import torch
except ImportError:
    raise SystemExit("未安装 PyTorch，请先按官方安装页配置当前环境。")

print("Python:", sys.version.split()[0])
print("OS:", platform.platform())
print("PyTorch:", torch.__version__)
print("CUDA available:", torch.cuda.is_available())

if torch.cuda.is_available():
    for i in range(torch.cuda.device_count()):
        prop = torch.cuda.get_device_properties(i)
        print(f"GPU {i}: {prop.name}; total memory={prop.total_memory / 2**30:.2f} GiB")

mps = getattr(torch.backends, "mps", None)
print("MPS available:", bool(mps is not None and mps.is_available()))

# 最小 CPU 自动求导检查，与 GPU 是否可用无关。
x = torch.tensor([1.0, 2.0, 3.0], requires_grad=True)
loss = x.square().mean()
loss.backward()
assert x.grad is not None and torch.isfinite(x.grad).all()
print("CPU autograd check: passed")
```

进入项目 B 后新建后训练环境，再按选定仓库安装 Transformers、Datasets、PEFT、TRL；记录实际版本。不要把所有教程不同年代的 `requirements.txt` 合并到同一个环境，也不要在实验中途整体升级。

建议用仓库 lockfile；没有 lockfile 时保存 `pip freeze`、Python/PyTorch 版本、模型 revision 和 Git commit。本文不是已在你的机器上验证过的依赖锁文件。

### 7.2 建议项目目录

```text
llm-study/
├── README.md
├── configs/
│   ├── tiny_pretrain.yaml
│   ├── sft_lora.yaml
│   ├── dpo.yaml
│   └── rl_toy.yaml
├── src/
│   ├── tokenizer.py
│   ├── data.py
│   ├── attention.py
│   ├── model.py
│   ├── train.py
│   ├── generate.py
│   ├── cache.py
│   └── evaluate.py
├── posttrain/
│   ├── train_sft.py
│   ├── train_dpo.py
│   └── train_rl_toy.py
├── agent/
│   ├── environment.py
│   ├── tools.py
│   └── run.py
├── tests/
├── data/
│   ├── raw/
│   ├── processed/
│   └── manifests/
├── runs/
├── reports/
└── references/
```

该目录是实施目标，不是随本文件交付的现成源码。课程代码从官方仓库取得，自己的实现另建目录；最终提交说明哪些部分原创、哪些部分改编。

### 7.3 每次实验记录

| 字段 | 应记录内容 |
|---|---|
| 实验目的 | 本次只验证哪个假设 |
| 版本 | 代码 commit、依赖、模型 revision、tokenizer/模板版本 |
| 数据 | 来源、许可、文件哈希、划分方式、样本/token 数 |
| 配置 | 模型结构、上下文、batch、累积、学习率、精度、LoRA 参数 |
| 资源 | 设备、内存峰值、训练时间、采样时间、评估时间 |
| 结果 | 固定评估集指标、样例、失败类别 |
| 对照 | 哪个变量变了，哪些保持不变 |
| 限制 | 随机性、小样本、没跑完的阶段、无法验证的推论 |

保存每个实验的配置和日志，不只保存“最好的权重”。测试集只用于最终或预先约定的评测；日常调参使用开发集。

### 7.4 每周复盘模板

```markdown
# 第 N 周复盘

## 本周目标与完成层级
L1 / L2 / L3：

## 已完成
代码、测试、实验：

## 输入、处理、输出
本模块与上下游如何衔接：

## 实验结果
对照配置、固定数据、主要指标：

## 失败与定位
复现方式、原因、修复或未解决部分：

## 资源消耗
设备、内存、耗时：

## 下周最小目标
只写一个最关键的交付物：
```

<a id="s8"></a>
## 八、资源控制与故障排查

### 8.1 控制成本的顺序

优先缩短输入/输出和降低单次 batch，其次缩模型，再考虑 LoRA、量化、梯度检查点等办法。每增加一个技巧，先保证可以解释它改变了什么。

| 方法 | 本计划怎么用 | 容易误解的地方 |
|---|---|---|
| 缩短序列与输出 | 从 128/256 上下文、64 输出开始 | “模型能支持长上下文”不等于训练也应使用最大长度。 |
| micro-batch 设为 1 | 然后用梯度累积控制有效 batch | 累积不能救下一个本身就放不进内存的 micro-batch。 |
| LoRA | 优先冻结基础权重，训练小增量 | 不会消除基础权重和所有激活的内存占用。 |
| QLoRA | 只在平台支持并验证后使用 | 权重量化体积不是总训练内存；算子兼容另查。[D5] |
| 梯度检查点 | 本机内存受限时试用 | 需要重新计算，通常以额外计算换内存；实际收益实测。 |
| DPO 参考概率缓存 | 固定参考模型、数据和预处理后研究使用 | 参考模型/模板改变需重算；版本存在配置限制。[D3] |
| 减少在线采样 | RL 先短任务、少候选、少步数 | 降低到奖励无差异，可能几乎没有学习信号。[D4] |

与具体工具有关的行为，以 [PEFT][D2]、[量化][D5]、[DPO][D3]、[GRPO][D4] 的所用版本文档为准。

### 8.2 每个新实验先做四步检查

```text
1. 读取少量样本，打印处理后的文本、token 和 mask。
2. 执行一次前向和反向，检查梯度及内存。
3. 跑 20 步，保存并重新加载。
4. 测量速度后，再决定完整训练预算。
```

数据量很小、模型输出很差时，也应先检查上述链路，不能直接归因于模型太小。

### 8.3 云 GPU 作为选项，不作为开学前提

前期主线不依赖付费模型 API，也不把固定免费 GPU 配额当作条件。确实需要补充算力时，只为已经通过本地测试的实验设置上限，例如自行选择 100—300 元预算；这是止损建议，不是实时报价，也不保证覆盖全部阶段。

估算原则：

```text
普通训练大致时长 ≈ 待处理 token 数 / 同配置实测训练 tokens/s
总成本 = 训练 + 在线采样 + 评估 + 启停/存储等成本
```

在线 RL 不能只用普通 SFT 的速度估算；生成常常需要单独计时。租用前确认计费规则、停止条件、检查点存储和数据授权。本计划不要求把内部业务数据上传到云端。

### 8.4 常见问题先排查什么

| 问题 | 优先检查 |
|---|---|
| loss 不下降 | 标签右移、loss mask、优化器是否更新、学习率、数据读取、梯度是否有限 |
| 训练 loss 很低但测试差 | 数据泄漏、训练测试分布、重复样本、小数据过拟合、错误评价 |
| SFT 后只会重复或输出异常格式 | chat template、EOS、截断位置、训练与推理提示差异 |
| CUDA 显存不足 | 序列长度、单次 batch、参考模型、缓存、保留计算图、日志保存张量 |
| DPO 没有效果或变差 | 偏好标签、参考模型身份、mask、beta、答案长度偏差 |
| RL 奖励上涨但实际更差 | 验证器漏洞、格式奖励过重、组内奖励无差异、题目泄漏 |
| 量化后速度没提升 | 硬件与算子支持、线程、batch、加载时间是否混入测量 |
| Agent 成功率低 | 工具 schema、任务难度、检索质量、动作解析、终止规则；先比较固定流程 |

这张表是排查顺序，不是对任何具体故障的诊断。

<a id="s9"></a>
## 九、结课验收与进阶方向

### 9.1 最终检查清单

- [ ] 能不依赖大模型框架，说明并实现微型模型的关键前向与训练过程。
- [ ] 分词、因果掩码、目标右移、保存恢复和缓存都有测试。
- [ ] 有一组真实预训练日志和一个数据/预算对照。
- [ ] 有已训练的小模型适配器；没有完成的实训如实标明。
- [ ] 能解释 SFT、LoRA、DPO、RLVR、GRPO 各自是什么，避免混用。
- [ ] 至少完成一次可验证奖励的真实参数更新；是否完成 GRPO 另行注明。
- [ ] 推理报告同时包含质量、速度、内存、配置。
- [ ] 本地工具项目有固定流程基线、证据验证和失败分析。
- [ ] 轨迹筛选后再训练没有污染最终测试集。
- [ ] 新环境能够按 README 跑通最小实验。

### 9.2 能够回答的十个问题

1. 为什么训练的目标序列必须右移？标签在哪一层移动？
2. 因果掩码和 padding mask 有什么区别？
3. 同一个 batch 的前向、反向、梯度累积和参数更新各何时发生？
4. 训练 loss 下降为什么不能单独证明泛化提升？
5. LoRA 冻结了哪些权重？量化后仍有哪些内存开销？
6. DPO 的参考模型是谁？为什么 chosen/rejected 必须对应同一问题？
7. 可验证奖励如何变成参数更新？只评分为什么不算强化学习？
8. KV cache 保存什么？位置和长度错了会如何影响结果？
9. 为什么工具调用失败不能都归因于模型推理能力？
10. 筛选合成数据、轨迹 SFT 与在线 Agent-RL 的区别是什么？

### 9.3 完成后只选一条进阶线

| 方向 | 建议增加的实验 | 所需条件 |
|---|---|---|
| 训练系统 | Triton 内核、通信测量、多卡训练 | 合适 GPU、多卡环境或可控租用预算；对应 CS336 Systems。[A2] |
| 后训练研究 | 固定任务、同一 SFT 起点下比较 DPO 与 RLVR，补多个种子和采样成本 | 能运行小模型在线采样；明确任务、奖励和数据预算。 |
| 持续学习 | 顺序任务 A→B、回放或适配器策略，测新任务收益与遗忘 | 清晰任务定义与独立测试，不把外挂记忆误当参数学习。 |
| 大模型与传统机器学习结合 | 离线抽标签/语义特征，再输入逻辑回归或树模型 | 现有分类/排序任务、时间或实体级切分、可复核基线。 |

最后一条可衔接你此前关注的“大模型生成特征，再进入传统模型”的方向。建议对比：传统特征、传统特征加 LLM 特征、直接 LLM 判断。生成特征时不能偷看目标标签或测试答案；本计划不预设哪条路线一定更好。

**开始执行时，只做第 1 周。不要先下载所有仓库、所有模型和全部语料。**

<a id="s10"></a>
## 十、课程、代码和官方文档入口

下列为本次核查所用入口，核查日期为 **2026 年 9 月 18 日**。在线资料会更新；课程代码、模型、依赖分别固定版本。页面可访问不表示本计划已执行该仓库所有训练，也不表示已核验其全部硬件配置。

### 10.1 课程与项目

- **R1｜Raschka：** [LLMs-from-scratch][R1]。主线代码按 `ch02`—`ch07` 及 `appendix-A`、`appendix-E` 导航。
- **R2｜CS336：** [2026 课程官网][R2]。录像入口、讲义和作业以官网为准。
- **R3｜smol-course：** [官方仓库][R3]。按指令训练、评价和偏好优化主题选用。
- **R4｜MiniMind：** [作者仓库][R4]。用于对照学习端到端实现。
- **R5｜Happy-LLM：** [官方仓库][R5]。中文原理与训练实践。
- **R6｜唐杰高级机器学习：** [教师官方主页][R6]。这是教师主页，**不是已确认的 2026 新版课程下载页**。
- **R7｜Karpathy：** [Neural Networks: Zero to Hero][R7]。
- **R8｜Hugging Face：** [LLM Course][R8]。
- **R9｜李宏毅：** [Machine Learning 2026 Spring][R9]。

### 10.2 CS336 作业代码

- **A1｜基础实现：** [assignment1-basics][A1]。
- **A2｜系统：** [assignment2-systems][A2]。
- **A3｜规模规律：** [assignment3-scaling][A3]。不默认课程训练 API 对校外学习者可用。
- **A4｜数据：** [assignment4-data][A4]。
- **A5｜对齐与推理训练：** [assignment5-alignment][A5]。选做部分以课程官网当期链接为准。

CS336 是实现密集型课程，官网建议先在 CPU 上调试正确性，再用 GPU 完整训练或测试性能。本计划采用缩小实验，不把原版作业规模作为硬件采购要求。[R2]

### 10.3 工具与模型

- **D1：** [TRL SFT Trainer][D1]。
- **D2：** [PEFT LoRA][D2]。
- **D3：** [TRL DPO Trainer][D3]。
- **D4：** [TRL GRPO Trainer][D4]。
- **D5：** [Transformers bitsandbytes 量化说明][D5]。
- **D6：** [llama.cpp][D6]。
- **D7：** [MLX-LM][D7]。
- **D8：** [PyTorch 官方安装选择器][D8]。
- **M1：** [SmolLM2-135M-Instruct 模型卡][M1]。
- **M2：** [Qwen3-0.6B 模型卡][M2]。

---

## 执行摘要

**前 6 周写懂训练，7—10 周理解并实践后训练，11—12 周学推理和资源测量，13—16 周做有验证、有轨迹、有再训练的本地项目。**

不追求把所有课程学完，不追求复现最大规模，也不预设每次训练都能提升效果。最终要留下的是一套可运行、可测试、可解释、可复现的学习成果。

[R1]: https://github.com/rasbt/LLMs-from-scratch
[R2]: https://cs336.stanford.edu/
[R3]: https://github.com/huggingface/smol-course
[R4]: https://github.com/jingyaogong/minimind
[R5]: https://github.com/datawhalechina/happy-llm
[R6]: https://keg.cs.tsinghua.edu.cn/jietang/
[R7]: https://github.com/karpathy/nn-zero-to-hero
[R8]: https://huggingface.co/learn/llm-course/chapter1/1
[R9]: https://speech.ee.ntu.edu.tw/~hylee/ml/2026-spring.php
[A1]: https://github.com/stanford-cs336/assignment1-basics/tree/main
[A2]: https://github.com/stanford-cs336/assignment2-systems/tree/main
[A3]: https://github.com/stanford-cs336/assignment3-scaling/tree/main
[A4]: https://github.com/stanford-cs336/assignment4-data/tree/main
[A5]: https://github.com/stanford-cs336/assignment5-alignment/tree/main
[D1]: https://huggingface.co/docs/trl/sft_trainer
[D2]: https://huggingface.co/docs/peft/v0.20.0/package_reference/lora
[D3]: https://huggingface.co/docs/trl/dpo_trainer
[D4]: https://huggingface.co/docs/trl/grpo_trainer
[D5]: https://huggingface.co/docs/transformers/quantization/bitsandbytes
[D6]: https://github.com/ggml-org/llama.cpp
[D7]: https://github.com/ml-explore/mlx-lm
[D8]: https://pytorch.org/get-started/locally/
[M1]: https://huggingface.co/HuggingFaceTB/SmolLM2-135M-Instruct
[M2]: https://huggingface.co/Qwen/Qwen3-0.6B
